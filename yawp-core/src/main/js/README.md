YAWP! Javascript client
=======================

Javascript client for YAPW! framework.

#### Usage

- If you use npm, `npm install yawp-cli --save`

- If not, pick the library directly from `dist` directory 
